// Global cursor elemenst
export const CURSOR = {
  cursorRound:document.querySelectorAll('.cursor-round'),
  cursorDot:document.querySelectorAll('.cursor-dot'),
}

// Distorion images
export const DISTORION = {
   distortion: 'distorion'
}

// Smooth Scroll Parameters
export const SCROLL ={
   body:document.body,
   scrollWrap: document.getElementsByClassName("scroller-wrapper")[0],
   speed:0.045,
   offset:0,
   callscroll:null
}

// Hamberger consts
export const HAMBERGER = {
   hamberger:'hamburger',
   hambergActive:'is-active',
   navmenu:'home-navigations',
   innernav:'inner-navigations-items'
}


// Conact appointment
export const CONTACT_CONST = {
   radioBtns:'radio-btn',
   btns:'appoint-men',
   modal:'modal',
   close:'modal-close'
}

// Dropdwon Constants
export const DROPDOWNS = {
   dropdown:'dropdown',
   dropdownTitle:'title',
   dropdownOptions:'option',
   dropMenu:'menu'
}